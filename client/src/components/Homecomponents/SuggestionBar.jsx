
import { memo } from "react"
const SuggestionBar=({debounceQuery})=>{
         
            return(
                <div className="">
                    <div>
                       suggested
                    </div>
                </div>
            )
}

export default memo(SuggestionBar)